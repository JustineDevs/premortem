# Cloudflare Skills

This category mirrors the official Cloudflare skill pack from `cloudflare/skills`.

Included skills:

- `cloudflare`
- `agents-sdk`
- `cloudflare-email-service`
- `durable-objects`
- `sandbox-sdk`
- `turnstile-spin`
- `web-perf`
- `workers-best-practices`
- `wrangler`

Why it belongs:

- Premortem uses Cloudflare Workers, Cloudflare Pages, and Cloudflare platform primitives for edge delivery and runtime support.
- The official Cloudflare skill pack covers the exact platform surface the project needs, so it is a direct vendor match rather than a local rewrite.
