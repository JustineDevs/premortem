version: 1
name: dedupe-policy
finding_level:
  same_finding_type_weight: 0.25
  same_asset_weight: 0.25
  same_failure_mode_weight: 0.20
  evidence_overlap_weight: 0.15
  dedupe_key_overlap_weight: 0.15
  threshold: 0.70
issue_level:
  title_normalization: true
  same_owner_category_weight: 0.25
  same_remediation_scope_weight: 0.25
  trigger_overlap_weight: 0.20
  evidence_overlap_weight: 0.15
  asset_overlap_weight: 0.15
  threshold: 0.65
parent_issue_rules:
  merge_if_same_root_cause: true
  split_if_different_owners: true
  split_if_different_blast_radius: true
