apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: premortem-agent-builder
  namespace: {{ .Values.global.namespace | quote }}
  labels:
{{ include "premortem.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: premortem-agent-builder
