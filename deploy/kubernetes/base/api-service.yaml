apiVersion: v1
kind: Service
metadata:
  name: premortem-api
  namespace: premortem
  labels:
    app.kubernetes.io/name: premortem-api
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: premortem-api
  ports:
    - name: http
      port: 80
      targetPort: http
