apiVersion: v1
kind: ResourceQuota
metadata:
  name: premortem-runtime-quota
  namespace: premortem
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 6Gi
    limits.cpu: "8"
    limits.memory: 12Gi
    pods: "12"
    persistentvolumeclaims: "2"
