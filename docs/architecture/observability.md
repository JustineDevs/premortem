# Observability

- Structured logs with correlation IDs.
- Traces for audit run, agent run, publish, reconcile, and webhook flows.
- Metrics for latency, error rate, queue depth, model spend, token usage, publish success, and retry count.
- Alerts for failed audits, stuck queues, provider auth expiry, and reconciliation drift.
