{
  "name": "@premortem/evals",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "pnpm --filter @premortem/db build && tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @premortem/db build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@premortem/agent-kit": "workspace:*",
    "@premortem/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
