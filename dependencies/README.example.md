# Dependencies

Use this folder for private vendor drops, local dependency archives, patched third-party bundles, and non-public integration packages.

Public package management should still flow through pnpm workspaces; this folder is for exceptional cases only.
